#!/usr/bin/env bash

set -euo pipefail

echo "== Zeitwerk =="
bin/rails zeitwerk:check

echo
echo "== RuboCop =="
bundle exec rubocop

echo
echo "== Dependency audit =="
bundle exec bundle-audit check

echo
echo "== Tests =="
bin/rails test
